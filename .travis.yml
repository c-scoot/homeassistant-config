language: python
python:
  - "3.5"
before_install:
  - mv travis_secrets.yaml secrets.yaml
install:
  - pip3 install homeassistant
script:
  - sudo mkdir /ssl/
  - touch /ssl/cert.pem
  - touch /ssl/key.pem
  - hass -c . --script check_config